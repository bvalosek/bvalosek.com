machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t bvalosek/bvalosek.com .

test:
  override:
    - echo ':)'

deployment:
  dockerhub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
      - docker push bvalosek/bvalosek.com

